# 游戏缺失界面补全计划

## 目标
盘点现有篮球放置经营游戏的界面覆盖，根据 GDD、原型和已有界面图确定缺口，并生成统一像素漫画风的缺失界面图。

## 成功标准
- 已有界面与 GDD 功能逐项对应。
- 缺失界面清单包含用途、关键状态和信息层级。
- 每个确认缺失的核心界面都有一张 16:9 中文高保真界面图。
- 输出统一存放并完成尺寸、数量、可读性和风格检查。

## 阶段
1. **项目与现有界面盘点** — complete
2. **确定缺失界面及统一视觉规范** — complete
3. **逐张生成缺失界面图** — complete
4. **验收、返修与交付索引** — complete
5. **建立球员品质颜色令牌与受影响界面清单** — complete
6. **返修所有含球员视觉的界面图** — complete
7. **品质颜色一致性验收** — complete
8. **盘点并分类全部可复用UI元素** — complete
9. **生成无文字白底UI合集图** — complete
10. **统一尺寸、验收与资产索引** — complete
11. **招募结果界面街球竞技与 MT 品质重设计** — complete
12. **复核当前 Cocos 其它界面及 SpriteFrame 规格** — complete
13. **建立可独立替换的球员品质素材体系** — complete
14. **重设计招募概率界面并验证品质展示** — complete
15. **按界面优先级继续重设计其它页面** — complete
16. **统一验收、尺寸检查与交付索引更新** — complete
17. **盘点 `新设计/游戏界面截图` 全部实机界面并与 Prefab 对应** — complete
18. **建立批量重设计母版、通用组件与界面清单** — complete
19. **生成除招募结果/招募概率外的全部 1080×2160 新原型图** — complete
20. **逐张目视验收、尺寸校验、总览与交付索引** — complete
21. **按 12 个新原型图拆解可复用 UI 元素与状态** — complete
22. **生成明亮街球竞技版无文字 UI 元素合图** — complete
23. **建立界面到元素映射、切图坐标与资产索引** — complete
24. **逐张检查合图留白、可读性、尺寸与风格一致性** — complete
25. **审计新原型与 UI 素材中残留的矢量化表现** — complete
26. **建立可重复的整数像素网格与参考图有限色板输出流程** — complete
27. **以招募结果为母版重生成其余原型、UI 合图及品质相关素材** — complete
28. **逐张检查像素边缘、文字可读性、尺寸与切图坐标** — complete

## 约束
- 只补当前设计真正需要的界面，不扩张新系统。
- 沿用现有像素风、篮球经理题材与中文界面。
- 关键操作位于安全区内；正文不使用过小字号；颜色之外必须有图标或文字编码。
- 生成图是视觉规格稿，不把不可控的生成文字视为最终可直接切片文本资源。
- 新设计原型统一为 1080×2160；不改变现有节点布局，动态文字继续由 Label 承载。
- 与品质相关的卡片背景、边框、标签、名牌和装饰件必须单独输出，不只存在于完整界面合成图中。

## 错误记录
| 错误 | 次数 | 处理 |
|---|---:|---|
| PowerShell 管道内 Python 的中文目录字面量乱码，未找到现有界面图 | 1 | 改为从工作区枚举包含 PNG 的目录，不在脚本中硬编码中文目录名 |
| PowerShell 管道内 Python 再次使用中文品质文件夹字面量，枚举报告 MISSING | 1 | 改为遍历球员 UI 下全部子目录，不在 Python 源码中写中文文件夹名 |
| 返修验收脚本再次使用中文路径字面量，Pillow 无法打开 | 1 | 保留已成功覆盖的文件；验收改用目录枚举与文件序号，不再传中文路径字面量 |
| UI合集总览输出名使用中文字面量，Pillow 保存失败 | 1 | 改用ASCII文件名 `ui_element_sheets_overview.png` |
| Memory 检索把 `D:\篮球` 作为正则模式导致转义错误 | 1 | 改用 `Select-String -SimpleMatch` 与模式数组 |
| PowerShell 当前环境缺少 `System.Drawing.Image` 类型，首次尺寸表无有效宽高 | 1 | 改用 Pillow 解码 13 个 PNG，并核验 12 张正式原型均为 1080×2160 RGBA |
| UI 合图脚本条件色值的星号解包语法错误 | 1 | 给条件表达式加括号后重新生成 |
| 快进图标将三角形坐标写成嵌套扁平元组 | 1 | 拆成两个标准点列表多边形后重新生成 |
| 内联验收脚本两次硬编码中文工具路径/清单名导致导入或读取失败 | 2 | 从 `Path.cwd()/tools` 导入并用 `glob('*.json')` 定位清单 |

## 2026-08-02 全部剩余界面重设计成功标准
- `新设计/游戏界面截图` 中每张完整界面都有对应的新原型图，已完成的招募结果与招募概率只复制/归档，不重复偏离已确认版本。
- 所有新原型图严格为 1080×2160，保持截图和对应 Prefab 的节点布局、功能语义、按钮数量与动态文字位置。
- 统一使用已确认的明亮街球竞技视觉：蓝白主面、酸性绿主操作、青色信息、橙红风险/对手；球员品质继续使用九套独立品质资产。
- 输出统一放入 `新设计/新原型图`，同时生成总览、文件清单和尺寸验收报告。

## 2026-08-02 UI 元素合图成功标准
- 覆盖 12 张正式原型中实际出现的通用底板、按钮/导航、数据状态、球员卡槽、比赛 HUD、弹窗奖励、系统图标，以及九档品质组件。
- 合图使用纯白背景、无文字、无动态数值；每个元素彼此留有切图安全间距，并提供 JSON 坐标与界面映射说明。
- 输出到 `新设计/UI元素合图`，不覆盖旧的 `outputs/ui_element_sheets*` 与 `品质素材_9档`。
- 所有合图统一 2048×2048 RGBA PNG，并生成总览图完成目视检查。

## 2026-08-02 全部改回像素风成功标准
- 12 张正式新原型、8 张 UI 元素合图、九档品质资源及招募替换资源统一使用整数像素网格、最近邻放大、有限色板和硬 alpha 边缘。
- 保留已确认的年轻街球竞技配色、MT 启发式品质层级、原布局、按钮数量和动态文字位置。
- 所有原型文字改用项目 `zpix.ttf`；不得继续使用微软雅黑等平滑字体。
- 返修前文件非破坏性备份；返修后尺寸、格式、合图坐标与元素零重叠验收仍需通过。
## 2026-07-07 细拆版 UI 元素返修
- **阶段 11：按可复用零件重新拆分 UI 元素** — complete
- 成功标准：按钮底板与 icon 分离；进度条框、填充、端帽、刻度、指针分离；品质框与头像/文字分离；输出纯白背景合集图并保留清单。
- 输出目录：`outputs/ui_element_sheets_refined/`
- 生成文件：8 张 2048×2048 细拆图、1 张总览图、1 个 `INDEX.md` 清单。
- 验收：全部 PNG 角落为纯白背景；新版未覆盖旧版 `outputs/ui_element_sheets/`。
- 新增错误记录：首次绘制脚本把 `circle(..., w=)` 误写为不存在的参数；已改为 `lw=` 并抽成稳定生成脚本 `tools/generate_refined_ui_sheets.py`。
