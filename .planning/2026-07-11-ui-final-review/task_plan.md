# 终稿 UI 复审、优化与元素图集

## 目标
- 审查 `界面图合集/终稿` 中 9 张竖屏 UI，保持全部功能与元素不变，仅改善可读性、安全区、触控尺寸、对齐和重复信息一致性。
- 输出完整中文命名高清界面集到 `界面图合集/终稿优化`。
- 输出 7 张无文字、纯洋红 `#FF00FF` 背景、可独立抠取的 UI 元素图集到 `界面图合集/终稿UI图集`。

## 阶段
1. **终稿逐张审查与功能不变量登记** — complete
2. **低分辨率界面高清重绘与高分辨率界面整理** — in_progress
3. **样板图集生成和抠图验证** — pending
4. **其余 6 张 UI 图集生成** — pending
5. **尺寸、命名、文字、背景和功能完整性验收** — pending

## 约束
- 原始 `终稿` 目录只读，不覆盖原图。
- 不删除、不增加、不改变任何功能、按钮、信息模块或入口。
- 图集只收录 9 张终稿实际使用的可复用 UI 组件，不收录头像、完整场景和人物插画。
- 图集不含文字或数字，元素不粘连，洋红背景必须统一。

## 错误记录
| 错误 | 次数 | 处理 |
|---|---:|---|
| 旧根目录计划文件存在乱码，按中文锚点追加失败 | 1 | 改用独立 `.planning/2026-07-11-ui-final-review/`，不改写旧文件 |
