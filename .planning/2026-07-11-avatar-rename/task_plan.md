# 147 张球员头像对应与改名

## 目标
- 以 `outputs/pixel_comic_headshots/sheet_manifest.csv`、`headshots_official/manifest.csv` 和 `data/player_config_fame_v3.csv` 为权威链路，将拆图头像改为 `游戏显示名_英文原名.png`。
- 不凭脸猜测；图像匹配无法可靠确认时停止对应并列入待确认。

## 阶段
1. **盘点拆图与名单并建立权威映射链** — complete
2. **用图像特征匹配每个拆图到合图槽位** — complete
3. **生成映射清单并验证唯一性与置信度** — complete
4. **非破坏性备份后批量改名** — complete
5. **检查数量、重复、非法字符和可回滚性** — complete

## 已确认
- 拆图共 147 张，六批数量为 25、25、25、25、25、22。
- sheet manifest、官方头像 manifest、游戏配置三者均覆盖 147 人，无缺失、无中文显示名冲突。
- 六个时间戳文件夹依次对应合图 1～6，但批内 `player_N` 不是行列顺序，必须逐图匹配。

## 错误记录
| 错误 | 次数 | 处理 |
|---|---:|---|
| Python 直接使用中文绝对路径时被控制台编码替换 | 2 | 改用工作目录下相对路径和文件枚举 |
| 按相同 slot 比较失败，因为拆图工具打乱批内顺序 | 1 | 改用 SIFT 特征匹配与全局一对一分配 |
| 附加抽查脚本直接使用中文相对路径失败 | 1 | 改用 PowerShell 原生路径读取映射表，不影响主映射结果 |
