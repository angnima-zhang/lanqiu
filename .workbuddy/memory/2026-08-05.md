# 2026-08-05

## Shader: 头像框闪电缠绕效果

- 创建了 `frame-lightning.effect` — 纯 shader 实现闪电缠绕，无需贴图
  - 技术：FBM 噪声 + 各向异性 UV 拉伸 + alpha 边缘检测 + 阈值切出闪电形状
  - 支持参数：boltColor, glowColor, boltSpeed, boltWidth, glowWidth, jaggedness, intensity, density
  - 路径：`assets/resources/effects/frame-lightning.effect`

- 创建了 `FrameLightning.ts` — TypeScript 集成工具
  - 单例加载 EffectAsset
  - `installFrameLightning(sprite, frameIndex)` 一键安装
  - 9 档品质预设（frame 0-8）：金色/紫金/紫/红/蓝/绿/银白/铜/灰暗
  - `applyLightningPreset(material, frameIndex)` 单独设置品质配色
  - 路径：`assets/scripts/home/FrameLightning.ts`

- 接入球员详情页面-总评节点（HomeUiController.ts）
  - 节点路径：`球员详情页面 → 总评 → 总评背景板`（cc.Sprite, 960x333）
  - `start()` 预加载 effect，`renderDetailedPlayerCard` 中 apply，`onDestroy` 清理
  - 根据球员品质自动切换闪电配色预设
  - 修改文件：`assets/scripts/home/HomeUiController.ts`（+Material import, +onDestroy, +applyLightningToOverallRating）
- 待用户放入 Cocos Editor 编译验证效果

---

## Shader: 招募按钮扫光+置换效果（替换旧呼吸发光）

- 创建 `recruit-button-sweep.effect`，两个效果叠加：
  1. **AE 置换图**：采样原图 → 计算亮度 → 亮度偏移 UV（亮处偏一个方向，暗处偏反方向）→ 重采样
  2. **对角扫光**：斜向光柱周期性扫过按钮表面
- 修改 `RecruitmentController.ts`：
  - 路径：`effects/recruit-button-glow` → `effects/recruit-button-sweep`
  - 全量重命名：installRecruitButtonGlow→Effect, sync→Effect, load→Effect, GlowMaterial→EffectMaterial
  - 旧属性移除：spriteRect, shineColor, pulseParams
  - 新属性：sweepColor, sweepParams(宽/周期/斜率/强度), displaceParams(强度/方向XY)
- 旧 glow effect 保留，RosterSlotView 仍在使用
- 安装 shader-graph 插件（后确认为 3D 专用，不适用于 2D sprite，待手动删除）

## 调参速查

| 文件 | 调什么 | 关键值 |
|---|---|---|
| `frame-lightning.effect` 默认属性 | boltWidth/glowWidth/density/speed | 全局生效 |
| `FrameLightning.ts` PRESETS | boltColor/glowColor/intensity/jaggedness | 按品质 0-8 分档 |
| `recruit-button-sweep.effect` 默认属性 | sweepWidth/Period/Slope, displaceStrength | 全局生效 |
| `RecruitmentController.ts` installRecruitButtonEffect | sweepParams/displaceParams | 按钮专用 |
